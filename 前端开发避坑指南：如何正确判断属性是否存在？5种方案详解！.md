# 前言
如何判断一个属性是否存在？有哪些方案？本文将为你一一揭晓。
# 布尔判断
例如判定obj对象中是否存在a属性，可以用布尔判断，但是布尔判断可能会携带一些问题，比如obj对象中可能存在a属性，但是值为“假值”，此时布尔判断就会出错。
```js
const obj={a:0}
if(obj.a){
    console.log('存在')
}else{
  console.log('不存在')
}
```
上述代码会打印不存在，因为属性a的值为0，在布尔判断中，0会被认为是false。
> 在布尔判断中，0、""、null、undefined、NaN、false会被认为是false，其他值都会被认为是true。

# in操作符
in操作符可以判断对象或对象原型链中是否存在某个属性。
##  用法
```js
属性名 in 对象
```
## 代码示例

```js
      const obj = { name: "Alice", age: 25 };

      console.log("obj", obj);
      
      if ("name" in obj) {
        console.log("name 属性存在");
      } else {
        console.log("name 属性不存在");
      }

      if ('toString' in obj) {
        console.log("toString 属性存在");
      } else {
        console.log("toString属性不存在");
      }
```
![](../1in操作符会遍历原型链属性.png)
# Object.hasOwn方法
如果只判断对象自身是否存在某个属性（不遍历原型链），可以使用hasOwnProperty方法。

目前推荐使用hasOwn()方法取代hasOwnProperty()方法，因为如果对象重写hasOwnProperty()方法可能会返回与预期不符的结果。
```js
const foo = {
  hasOwnProperty() {
    return false;
  },
  bar: "Here be dragons",
};

foo.hasOwnProperty("bar"); // 该重新实现始终返回 false

```
## 用法
```js
Object.hasOwn(obj, prop)
```
## 代码示例
```js
const person = { name: 'jack', age: 25 };

if (Object.hasOwn(person,'name')) {
  console.log('name 属性存在');
} else {
  console.log('name 属性不存在');
}
```
打印name属性存在。如果name属性为null或者undefined或者NaN，空字符串，打印name也是存在的。

# 使用undefined检查
通过检查属性值是否是undefined来判断属性是否存在。
```js
const obj = { name: "Alice", age: 25 };

if (obj.name !== undefined) {
  console.log("name 属性存在");
} else {
  console.log("name 属性不存在");
}
```
# 可选链操作符（?.）
可选链操作符（?.）可以用来判断对象是否存在某个属性，如果存在则返回属性值，如果不存在则返回undefined。
```js
const obj = { name: "Alice", age: 25 };

const name = obj?.name;
console.log(name); // Alice
```
如果obj对象中不存在name属性，则name的值为undefined。
# 使用Object.keys()方法
Object.keys()方法可以返回一个数组，数组中包含了对象自身的所有可枚举属性名。
```js
      const obj = { name: "Alice", age: 25 };
      const fruits = { name: "apple", size: 10, color: "red" };
      if (Object.keys(obj).includes("name")) {
        console.log("name 属性存在");
      }

      console.log("fruits 属性", Object.keys(fruits));
      //上面一句打印fruits 属性 (3) ['name', 'size', 'color']
```
# Object.getOwnPropertyNames()方法
和Object.keys()方法类似，Object.getOwnPropertyNames()方法也可以返回一个数组，数组中包含了对象自身的所有属性名，包括不可枚举属性。
```js
      const fruits = { name: "apple", size: 10, color: "red" };

      console.log("fruits 属性", Object.getOwnPropertyNames(fruits));
      //上面一句打印fruits 属性 (3) ['name', 'size', 'color']
```

关于对象的可枚举属性和不可枚举属性，可以参考这篇文章：[前端开发避坑指南：如何正确判断属性是否存在？5种方案详解！](https://juejin.cn/post/7216219609166788287)
# Reflect.has()方法
Reflect.has()方法可以用来检查对象及其原型链中是否存在指定的属性。
```js
      const obj = { name: "Alice", age: 25 };
      console.log(Reflect.has(obj,'name'));//true
```
# 总结
1. 布尔判断：直接使用布尔值判断属性是否存在。

2. in 操作符：检查对象及其原型链中的属性。

3. hasOwnProperty：仅检查对象自身的属性。

4. undefined 检查：适用于属性值不为 undefined 的情况。

5. 可选链操作符 (?.)：安全地访问嵌套属性。

6. Object.keys 或 Object.getOwnPropertyNames：获取对象的所有属性名并检查。

7. Reflect.has：类似于 in 操作符，检查对象及其原型链中的属性。
