# 前言
本文从浏览器缓存是什么、工作原理、类型、优缺点、优先级等方面详细介绍了浏览器缓存，并给出了相关的实践建议。

# 浏览器缓存是什么
浏览器缓存（Browser Caching）是浏览器会存储访问过的页面的资源，当再次访问同一页面时，浏览器就会优先从缓存中加载这些资源，而不是从服务器重新下载，减轻服务器的负担，提高网站的性能。

# 浏览器缓存工作原理
浏览器缓存机制也叫HTTP缓存机制，根据HTTP报文的缓存标识进行的。
## HTTP请求报文

 
## HTTP响应报文


https://juejin.cn/post/6844903593275817998

https://www.nowcoder.com/feed/main/detail/da083431762c4dfbbae8921c55d7c872

https://cloud.tencent.com/developer/article/1907948

# 缓存的分类
浏览器缓存可以分为强缓存和协商缓存两种类型。

## 强缓存
强缓存是指浏览器在本地缓存中查找资源，如果找到了，就直接使用缓存，而不会向服务器发送请求。强缓存的标识是HTTP响应头中的Cache-Control和Expires字段。

Cache-Control字段可以设置以下值：
- private：表示资源只能被单个用户缓存，不能被其他用户共享。
- public：表示资源可以被所有用户缓存，包括代理服务器。
- no-cache：表示资源可以被缓存，但在使用之前必须向服务器验证。
- no-store：表示资源不能被缓存。


1. 缓存的分类
浏览器缓存一般分为两类：

强缓存（Strong Caching）：
在有效期内，浏览器直接从本地缓存中加载资源，而不与服务器进行任何通信。
常用的 HTTP 头字段：
Expires：指定资源的到期时间，是一个绝对时间（受客户端时间影响）。
Cache-Control：替代 Expires 的相对时间设置（比如 max-age=3600 表示资源在 3600 秒内有效）。
协商缓存（Conditional Caching）：
当强缓存失效时，浏览器会向服务器发送请求，验证缓存资源是否可用。若可用，服务器返回 304 状态码，浏览器从缓存中加载资源；否则返回 200，并提供最新资源。
常用的 HTTP 头字段：
Last-Modified 和 If-Modified-Since：基于资源的最后修改时间。
ETag 和 If-None-Match：基于资源内容的唯一标识符。
2. 缓存的流程
浏览器加载资源时，会按照以下流程判断是否使用缓存：

强缓存：
检查 Cache-Control 和 Expires，判断缓存是否仍然有效。
若有效，直接使用缓存，状态码为 200（来自缓存）。
协商缓存：
若强缓存失效，向服务器发送验证请求（带有 If-Modified-Since 或 If-None-Match）。
服务器判断缓存是否可用：
可用：返回 304，浏览器使用缓存。
不可用：返回最新资源，状态码为 200。
无缓存：
如果未命中任何缓存策略，浏览器直接向服务器请求资源，并存储新的缓存数据。
3. 缓存控制策略
开发者可以通过 HTTP 头配置缓存策略：

Cache-Control 常见指令：
public：允许所有用户（包括代理服务器）缓存。
private：仅允许浏览器缓存，代理服务器不可缓存。
no-cache：跳过强缓存，直接进入协商缓存。
no-store：不使用任何缓存，每次都从服务器请求。
max-age：指定资源缓存的有效时间（单位：秒）。
ETag 和 Last-Modified 的选择：
ETag 更精准，可以识别资源内容的细微变化。
Last-Modified 更高效，但可能受时间精度限制。
4. 缓存位置
浏览器缓存的存储位置包括：

内存缓存（Memory Cache）：存储短期使用的资源，如当前页面使用的内容。
磁盘缓存（Disk Cache）：存储长期使用的资源，通常用于页面重新加载或导航。
Service Worker 缓存：通过 Service Worker 实现更灵活的缓存策略，适用于离线场景。
5. 缓存失效的场景
以下情况会导致缓存失效：

缓存已过期（强缓存）。
资源内容发生变化（协商缓存验证失败）。
手动刷新页面（Ctrl+R）：跳过强缓存，直接协商缓存。
强制刷新页面（Ctrl+Shift+R）：跳过所有缓存，直接向服务器请求。
总结来说，浏览器的缓存机制是通过强缓存和协商缓存的组合，尽可能减少网络请求以优化性能。合理配置 HTTP 缓存策略可以显著提升网站的加载速度和用户体验。